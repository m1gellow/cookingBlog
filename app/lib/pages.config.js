class PageConfig{
    home = "/";
    about = "/about";
    contacts = "/contacts";
    gallery = "/gallery";
    catalog = "/catalog";

    newPage = '/'

  

}

export const pageConfig = new PageConfig();