import React from "react";

const SectionDescription = ({ text }: { text: string }) => {
  return (
    <div>
      <h3 >
        {text}
      </h3>
    </div>
  );
};

export default SectionDescription;
